# Complete path, not gold depth

A signals student asks for the convolution of two finite sequences with no
netlist. Name the capability (`signal-analysis` / `algebraic-check`). If no
provider checks it, the exact token `unchecked` is a successful co-solver
outcome — do not route through SPICE.
