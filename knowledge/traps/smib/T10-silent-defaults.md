# T10 — Silent wrong defaults (Pm, Pmax stages, PD)

**Bare-agent failures:** Pm=1, Pmax_fault=0, f0=60 in Exp6; wrong PD; goldens copied from PDF without running integrator.

**Arc path:** `validate_smib_case()`; goldens only from `artifacts/eec301-lab-goldens/*.json`.
