# Loop plan — `R1` — inventory boot

> Collapsed plan for **one loop node**. Parent: [LOOP_GRAPH.md](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `R1` |
| **Job** | Run inventory command; record docs/planning/R1_BOOT_KNOWLEDGE.md |
| **Wave** | 5 |
| **Depends on (data)** | E1 |
| **Write paths** | `docs/planning/R1_BOOT_KNOWLEDGE.md` |
| **Read paths** | `scripts/check_knowledge_tree.py` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **State** | [R1.state.json](R1.state.json) |
| **Isolation** | path-ownership; checker writes nothing |
| **Companion skills** | ponytail |

---

## Stop (required)

**Command:**

```text
grep -q 'check_knowledge_tree' docs/planning/R1_BOOT_KNOWLEDGE.md && grep -q 'exit' docs/planning/R1_BOOT_KNOWLEDGE.md
```


---

## Objective

Boot log names the command that was actually run, exit code, and observed unit/question counts.

## Non-goals

- Do not start product CLI as the boot of this graph

---

## Contract

**Input:**

```json
{}
```

**Maker output:** `{ "files_touched": [], "notes": "" }`

**Checker output:** `{ "pass": false, "command": "", "exit_code": 1, "findings": [] }`

---

## Escalate

After `max_rounds` failures, or if the checker cannot run the stop: **stop the loop**, set status `escalated`, wait for the human. Do not start the next dependent node.

---

## Commits (this node only)

| # | Commit | Contents | Gate |
|---|--------|----------|------|
| 17 | `test(knowledge): inventory boot trials and corpus README` | R1 log | `grep -q 'check_knowledge_tree' docs/planning/R1_BOOT_KNOWLEDGE.md && grep -q 'exit' docs/planning/R1_BOOT_KNOWLEDGE.md` |

---

## Do not

- Commit, push, or open a PR
- Write outside **Write paths**
- Checker: write product files, or be the same Task as the maker
- Expand into another graph
- Load sibling loop plans
- Touch `src/`, `ui/`, `workflows/`, `eval/gold/`, `docs/PID.md`, `docs/PRD.md`, `docs/ARCHITECTURE.md`, `docs/PRODUCT.md`
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files touched, notes. Checker: pass, command, exit code, findings.
Lead updates `R1.state.json`.
