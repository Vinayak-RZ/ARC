# Loop plan — `T1` — spot-check trials

> Collapsed plan for **one loop node**. Parent: [LOOP_GRAPH.md](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `T1` |
| **Job** | Five queued trials logged |
| **Wave** | 5 |
| **Depends on (data)** | R1 |
| **Write paths** | `docs/planning/T1_TRIALS_KNOWLEDGE.md` |
| **Read paths** | `knowledge/ug-ee/**` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **State** | [T1.state.json](T1.state.json) |
| **Isolation** | path-ownership; checker writes nothing |
| **Companion skills** | ponytail |

---

## Stop (required)

**Command:**

```text
python3 -c "t=open('docs/planning/T1_TRIALS_KNOWLEDGE.md').read(); assert t.count('| pass |')>=5 or t.lower().count('pass')>=5"
```


---

## Objective

T1 log has the queue: (1) circuits mesh/Thevenin numeric recompute (2) signals convolution or sampling (3) control Routh or Bode (4) machines transformer OC/SC (5) PE chopper/rectifier + one elective heading check.

## Non-goals

- Do not skip numeric recompute for numerical units

---

## Contract

**Input:**

```json
{}
```

**Maker output:** `{ "files_touched": [], "notes": "" }`

**Checker output:** `{ "pass": false, "command": "", "exit_code": 1, "findings": [] }`

---

## Escalate

After `max_rounds` failures, or if the checker cannot run the stop: **stop the loop**, set status `escalated`, wait for the human. Do not start the next dependent node.

---

## Commits (this node only)

| # | Commit | Contents | Gate |
|---|--------|----------|------|
| 17 | `test(knowledge): inventory boot trials and corpus README` | T1 log | `python3 -c "t=open('docs/planning/T1_TRIALS_KNOWLEDGE.md').read(); assert t.count('| pass |')>=5 or t.lower().count('pass')>=5"` |

---

## Do not

- Commit, push, or open a PR
- Write outside **Write paths**
- Checker: write product files, or be the same Task as the maker
- Expand into another graph
- Load sibling loop plans
- Touch `src/`, `ui/`, `workflows/`, `eval/gold/`, `docs/PID.md`, `docs/PRD.md`, `docs/ARCHITECTURE.md`, `docs/PRODUCT.md`
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files touched, notes. Checker: pass, command, exit code, findings.
Lead updates `T1.state.json`.
