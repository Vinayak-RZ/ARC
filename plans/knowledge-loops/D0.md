# Loop plan — `D0` — coverage manifest

> Collapsed plan for **one loop node**. Parent: [LOOP_GRAPH.md](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `D0` |
| **Job** | Write COVERAGE.yaml listing every required pack unit |
| **Wave** | 0 |
| **Depends on (data)** | P0 product lock |
| **Write paths** | `knowledge/COVERAGE.yaml` |
| **Read paths** | `docs/curriculum-map.md`, `knowledge/PRODUCT.md` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **State** | [D0.state.json](D0.state.json) |
| **Isolation** | path-ownership; checker writes nothing |
| **Companion skills** | ponytail |

---

## Stop (required)

**Command:**

```text
python3 scripts/check_coverage_floors.py
```


---

## Objective

COVERAGE.yaml enumerates every encyclopedic unit for curriculum-map packs plus electives.

## Non-goals

- Do not write unit notes yet

---

## Contract

**Input:**

```json
{}
```

**Maker output:** `{ "files_touched": [], "notes": "" }`

**Checker output:** `{ "pass": false, "command": "", "exit_code": 1, "findings": [] }`

---

## Escalate

After `max_rounds` failures, or if the checker cannot run the stop: **stop the loop**, set status `escalated`, wait for the human. Do not start the next dependent node.

---

## Commits (this node only)

| # | Commit | Contents | Gate |
|---|--------|----------|------|
| 4 | `feat(knowledge): add coverage manifest schema and tree checker` | COVERAGE.yaml (with scaffold) | `python3 -c "import yaml,sys; d=yaml.safe_load(open('knowledge/COVERAGE.yaml')); packs=d['packs']; assert len(packs)>=10; mins={'circuits':16,'signals':12,'electronics':14,'maths':10,'machines':11,'power':12,'control':12,'power-electronics':12,'measurements':7,'em':10};
[(_ for _ in [0] if all(len(packs[k]['units'])>=mins[k] for k in mins) or sys.exit(1))]"` |

---

## Do not

- Commit, push, or open a PR
- Write outside **Write paths**
- Checker: write product files, or be the same Task as the maker
- Expand into another graph
- Load sibling loop plans
- Touch `src/`, `ui/`, `workflows/`, `eval/gold/`, `docs/PID.md`, `docs/PRD.md`, `docs/ARCHITECTURE.md`, `docs/PRODUCT.md`
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files touched, notes. Checker: pass, command, exit code, findings.
Lead updates `D0.state.json`.
