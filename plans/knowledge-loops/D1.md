# Loop plan — `D1` — docs-out

> Collapsed plan for **one loop node**. Parent: [LOOP_GRAPH.md](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `D1` |
| **Job** | knowledge/README.md names R1 command; thin root README pointer |
| **Wave** | 5 |
| **Depends on (data)** | T1 |
| **Write paths** | `knowledge/README.md`, `README.md` (one paragraph pointer only) |
| **Read paths** | `docs/planning/R1_BOOT_KNOWLEDGE.md` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **State** | [D1.state.json](D1.state.json) |
| **Isolation** | path-ownership; checker writes nothing |
| **Companion skills** | readme |

---

## Stop (required)

**Command:**

```text
grep -q 'check_knowledge_tree' knowledge/README.md && grep -q 'knowledge/' README.md
```


---

## Objective

Corpus README names the inventory/boot command. Root README has a one-paragraph pointer, not a rewrite.

## Non-goals

- Do not rewrite the product README landing

---

## Contract

**Input:**

```json
{}
```

**Maker output:** `{ "files_touched": [], "notes": "" }`

**Checker output:** `{ "pass": false, "command": "", "exit_code": 1, "findings": [] }`

---

## Escalate

After `max_rounds` failures, or if the checker cannot run the stop: **stop the loop**, set status `escalated`, wait for the human. Do not start the next dependent node.

---

## Commits (this node only)

| # | Commit | Contents | Gate |
|---|--------|----------|------|
| 17 | `test(knowledge): inventory boot trials and corpus README` | README pointer | `grep -q 'check_knowledge_tree' knowledge/README.md && grep -q 'knowledge/' README.md` |

---

## Do not

- Commit, push, or open a PR
- Write outside **Write paths**
- Checker: write product files, or be the same Task as the maker
- Expand into another graph
- Load sibling loop plans
- Touch `src/`, `ui/`, `workflows/`, `eval/gold/`, `docs/PID.md`, `docs/PRD.md`, `docs/ARCHITECTURE.md`, `docs/PRODUCT.md`
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files touched, notes. Checker: pass, command, exit code, findings.
Lead updates `D1.state.json`.
