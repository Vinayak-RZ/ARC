# Loop plan — `H1` — `harden`

> Collapsed plan for **one loop node**. Parent: [LOOP_GRAPH.md](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `H1` |
| **Job** | Ponytail-audit; fix P0-blocking + V_SEC leftovers; else N/A note. |
| **Wave** | 11 |
| **Depends on (data)** | `T1 holes D1 readme` |
| **Write paths** | `scripts/validate.sh tests/** src/**` |
| **Read paths** | `docs/planning/PONYTAIL_REVIEW.md docs/planning/SECURITY_REVIEW.md docs/planning/T1_TRIALS.md` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | generalPurpose (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **State** | [H1.state.json](H1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | ponytail-audit |

---

## Stop (required)

```text
./scripts/validate.sh
```

---

## Objective

validate.sh 0 after audit cuts or Lean already.

## Non-goals

- gold-plating after trials

---

## Contract

**Input:**

```json
{ "from": "T1 holes D1 readme" }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "" }
```

**Checker output:**

```json
{ "pass": false, "command": "", "exit_code": 1, "findings": [] }
```

---

## Escalate

After `max_rounds` failures, or if the checker cannot run the stop: set `escalated`, wait for the human. Do not start dependents.

---

## Commits (this node only)

| # | Commit | Contents | Gate |
|---|--------|----------|------|
| | `chore: harden from trial holes` | write paths above | stop command |

---

## Do not

- Commit, push, or open a PR
- Write outside **Write paths**
- Checker: write product files, or be the same Task as the maker
- Expand into another graph
- Load sibling loop plans
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files touched, notes. Checker: pass, command, exit code, findings.
Lead updates `H1.state.json`.
