# Loop plan — `D0` — `docs-in`

> Collapsed plan for **one loop node**. Parent: [LOOP_GRAPH.md](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `D0` |
| **Job** | Accept D19/D20 for this graph, gap list, FR1-FR24 trace, speckit-analyze notes. |
| **Wave** | 1 |
| **Depends on (data)** | `P0 product_lock` |
| **Write paths** | `docs/planning/D0_GAPS.md docs/planning/FR_TRACE.md docs/PID.md docs/PRD.md docs/ARCHITECTURE.md` |
| **Read paths** | `docs/PID.md docs/PRD.md docs/ARCHITECTURE.md .specify/` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | generalPurpose (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **State** | [D0.state.json](D0.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | speckit-analyze |

---

## Stop (required)

```text
test -f docs/planning/D0_GAPS.md && grep -q 'docs accepted' docs/planning/D0_GAPS.md && test -f docs/planning/FR_TRACE.md && grep -q 'FR17' docs/planning/FR_TRACE.md
```

---

## Objective

Gap list and FR_TRACE exist; Proposed overlays accepted-this-graph.

## Non-goals

- README rewrite, ADR bodies

---

## Contract

**Input:**

```json
{ "from": "P0 product_lock" }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "" }
```

**Checker output:**

```json
{ "pass": false, "command": "", "exit_code": 1, "findings": [] }
```

---

## Escalate

After `max_rounds` failures, or if the checker cannot run the stop: set `escalated`, wait for the human. Do not start dependents.

---

## Commits (this node only)

| # | Commit | Contents | Gate |
|---|--------|----------|------|
| | `docs: accept D19/D20 and list gaps` | write paths above | stop command |

---

## Do not

- Commit, push, or open a PR
- Write outside **Write paths**
- Checker: write product files, or be the same Task as the maker
- Expand into another graph
- Load sibling loop plans
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files touched, notes. Checker: pass, command, exit code, findings.
Lead updates `D0.state.json`.
