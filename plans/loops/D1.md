# Loop plan — `D1` — `docs-out`

> Collapsed plan for **one loop node**. Parent: [LOOP_GRAPH.md](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `D1` |
| **Job** | Product README + EXTENSIVE after evidence; name R1 boot command. |
| **Wave** | 10 |
| **Depends on (data)** | `T1 trial_log` |
| **Write paths** | `README.md docs/EXTENSIVE.md tests/unit/test_readme_paths.py` |
| **Read paths** | `docs/planning/R1_BOOT.md docs/planning/T1_TRIALS.md docs/PRODUCT.md` |
| **Maker type** | generalPurpose |
| **Maker model** | inherit |
| **Checker type** | generalPurpose (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **State** | [D1.state.json](D1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | readme product-readme extensive-readme |

---

## Stop (required)

```text
grep -q electrical-engineer README.md && grep -q 'electrical-engineer' README.md
```

---

## Objective

README names the boot command that actually ran.

## Non-goals

- README in D0, marketing-only copy that contradicts trials

---

## Contract

**Input:**

```json
{ "from": "T1 trial_log" }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "" }
```

**Checker output:**

```json
{ "pass": false, "command": "", "exit_code": 1, "findings": [] }
```

---

## Escalate

After `max_rounds` failures, or if the checker cannot run the stop: set `escalated`, wait for the human. Do not start dependents.

---

## Commits (this node only)

| # | Commit | Contents | Gate |
|---|--------|----------|------|
| | `docs: README after boot and trials` | write paths above | stop command |

---

## Do not

- Commit, push, or open a PR
- Write outside **Write paths**
- Checker: write product files, or be the same Task as the maker
- Expand into another graph
- Load sibling loop plans
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files touched, notes. Checker: pass, command, exit code, findings.
Lead updates `D1.state.json`.
