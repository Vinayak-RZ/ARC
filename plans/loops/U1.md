# Loop plan — `U1` — `UX IA a11y`

> Collapsed plan for **one loop node**. Parent: [LOOP_GRAPH.md](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `U1` |
| **Job** | Two-band slots, UX states, keyboard/WCAG AA; DESIGN-coinbase unchanged. |
| **Wave** | 3 |
| **Depends on (data)** | `A1 layout_aci` |
| **Write paths** | `docs/ui-ia.md` |
| **Read paths** | `docs/design/DESIGN-coinbase.md docs/frontend-architecture.md docs/PRODUCT.md` |
| **Maker type** | generalPurpose |
| **Maker model** | inherit |
| **Checker type** | generalPurpose (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **State** | [U1.state.json](U1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | frontend-architecture impeccable product audit |

---

## Stop (required)

```text
grep -q evidentiary docs/ui-ia.md && grep -q argument.md docs/ui-ia.md && grep -q waiting-human docs/ui-ia.md && grep -qi 'WCAG' docs/ui-ia.md && grep -qi keyboard docs/ui-ia.md
```

---

## Objective

docs/ui-ia.md names evidentiary/argument/plan/observation plus empty/error/a11y.

## Non-goals

- restyle, impeccable teach, GSAP, Next.js

---

## Contract

**Input:**

```json
{ "from": "A1 layout_aci" }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "" }
```

**Checker output:**

```json
{ "pass": false, "command": "", "exit_code": 1, "findings": [] }
```

---

## Escalate

After `max_rounds` failures, or if the checker cannot run the stop: set `escalated`, wait for the human. Do not start dependents.

---

## Commits (this node only)

| # | Commit | Contents | Gate |
|---|--------|----------|------|
| | `docs(ui): two-band plan and observation slots` | write paths above | stop command |

---

## Do not

- Commit, push, or open a PR
- Write outside **Write paths**
- Checker: write product files, or be the same Task as the maker
- Expand into another graph
- Load sibling loop plans
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files touched, notes. Checker: pass, command, exit code, findings.
Lead updates `U1.state.json`.
