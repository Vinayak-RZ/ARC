# Loop plan — `R1` — `boot`

> Collapsed plan for **one loop node**. Parent: [LOOP_GRAPH.md](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `R1` |
| **Job** | Actually boot CLI, one attachment, capability/propose path, ui bind, empty UI. |
| **Wave** | 8 |
| **Depends on (data)** | `E1 suite V_SEC sec_note` |
| **Write paths** | `docs/planning/R1_BOOT.md` |
| **Read paths** | `src/electrical_engineer/cli.py` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | generalPurpose (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **State** | [R1.state.json](R1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | none extra |

---

## Stop (required)

```text
test -f docs/planning/R1_BOOT.md && grep -q electrical-engineer docs/planning/R1_BOOT.md
```

---

## Objective

R1_BOOT.md records commands and observed results.

## Non-goals

- README rewrite

---

## Contract

**Input:**

```json
{ "from": "E1 suite V_SEC sec_note" }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "" }
```

**Checker output:**

```json
{ "pass": false, "command": "", "exit_code": 1, "findings": [] }
```

---

## Escalate

After `max_rounds` failures, or if the checker cannot run the stop: set `escalated`, wait for the human. Do not start dependents.

---

## Commits (this node only)

| # | Commit | Contents | Gate |
|---|--------|----------|------|
| | `docs: boot log` | write paths above | stop command |

---

## Do not

- Commit, push, or open a PR
- Write outside **Write paths**
- Checker: write product files, or be the same Task as the maker
- Expand into another graph
- Load sibling loop plans
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files touched, notes. Checker: pass, command, exit code, findings.
Lead updates `R1.state.json`.
