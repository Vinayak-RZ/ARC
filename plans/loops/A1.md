# Loop plan — `A1` — `architecture ADRs`

> Collapsed plan for **one loop node**. Parent: [LOOP_GRAPH.md](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `A1` |
| **Job** | Accept ADR-0009/0010/0011; write ADR-0012 layout/trust/ACI/run/fail-closed. |
| **Wave** | 2 |
| **Depends on (data)** | `D0 gap_list_fr_trace` |
| **Write paths** | `DECISIONS.md` |
| **Read paths** | `docs/ARCHITECTURE.md docs/planning/D0_GAPS.md docs/PRODUCT.md` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | generalPurpose (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **State** | [A1.state.json](A1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | system-design-tradeoffs backend-architecture agentic-system-design |

---

## Stop (required)

```text
grep -q 'ADR-0012' DECISIONS.md && grep -qi 'trust' DECISIONS.md && grep -qi 'fail closed' DECISIONS.md
```

---

## Objective

DECISIONS.md has ADR-0012 and trust/fail-closed.

## Non-goals

- UX copy, visual tokens, new stack

---

## Contract

**Input:**

```json
{ "from": "D0 gap_list_fr_trace" }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "" }
```

**Checker output:**

```json
{ "pass": false, "command": "", "exit_code": 1, "findings": [] }
```

---

## Escalate

After `max_rounds` failures, or if the checker cannot run the stop: set `escalated`, wait for the human. Do not start dependents.

---

## Commits (this node only)

| # | Commit | Contents | Gate |
|---|--------|----------|------|
| | `docs(adr): freeze ACI artifacts and src ownership` | write paths above | stop command |

---

## Do not

- Commit, push, or open a PR
- Write outside **Write paths**
- Checker: write product files, or be the same Task as the maker
- Expand into another graph
- Load sibling loop plans
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files touched, notes. Checker: pass, command, exit code, findings.
Lead updates `A1.state.json`.
