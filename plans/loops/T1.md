# Loop plan — `T1` — `queued trials`

> Collapsed plan for **one loop node**. Parent: [LOOP_GRAPH.md](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `T1` |
| **Job** | Fill all 15 trial rows pass or N/A with reason + evidence. |
| **Wave** | 9 |
| **Depends on (data)** | `R1 booted` |
| **Write paths** | `docs/planning/T1_TRIALS.md` |
| **Read paths** | `docs/planning/R1_BOOT.md docs/PRODUCT.md` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | generalPurpose (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **State** | [T1.state.json](T1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | graph-of-loops QUALITY.md |

---

## Stop (required)

```text
test -f docs/planning/T1_TRIALS.md && grep -c '| pass \?|' docs/planning/T1_TRIALS.md | awk '{exit !($1>=1)}' && grep -q 'Happy P0' docs/planning/T1_TRIALS.md && grep -q 'unchecked' docs/planning/T1_TRIALS.md
```

---

## Objective

T1_TRIALS.md has the full queue filled.

## Trial queue (all rows required)

Fill `docs/planning/T1_TRIALS.md` with pass or `N/A — reason` + evidence for:

1. Happy P0 circuits attachment
2. Empty RAG + empty UI run list
3. Invented capability id rejected
4. Regression: unmatched no auto-spice; photo MCP `ui_url`
5. Auth: N/A
6. FR9 no mint
7. Two-band cannot flip `unchecked`
8. Non-circuits pack path or `unchecked` (no forced spice)
9. UI two-band on 127.0.0.1
10. MCP tools/list 5–7 including `propose_composition`
11. UX empty state: no crash, honest empty
12. UX error/fail-closed visible in UI
13. UX keyboard / a11y
14. waiting-human / photo confirm still no sim
15. `unchecked` badge exact token in UI or CLI JSON

## Non-goals

- skipping empty/error/UX rows

---

## Contract

**Input:**

```json
{ "from": "R1 booted" }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "" }
```

**Checker output:**

```json
{ "pass": false, "command": "", "exit_code": 1, "findings": [] }
```

---

## Escalate

After `max_rounds` failures, or if the checker cannot run the stop: set `escalated`, wait for the human. Do not start dependents.

---

## Commits (this node only)

| # | Commit | Contents | Gate |
|---|--------|----------|------|
| | `docs: queued trial log` | write paths above | stop command |

---

## Do not

- Commit, push, or open a PR
- Write outside **Write paths**
- Checker: write product files, or be the same Task as the maker
- Expand into another graph
- Load sibling loop plans
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files touched, notes. Checker: pass, command, exit code, findings.
Lead updates `T1.state.json`.
