# Loop plan — `P0` — product lock

> Extensive node plan for kernel-harden graph. Parent: [`LOOP_GRAPH.md`](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `P0` |
| **Job** | product lock |
| **Wave** | 0 |
| **Depends on (data)** | none |
| **Write paths** | `docs/planning/PRODUCT_KERNEL_HARDEN.md docs/PRODUCT.md` |
| **Read paths** | `docs/PRODUCT.md docs/planning/GATE_0_KERNEL_HARDEN.md` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 2 |
| **Inner critique loop** | no |
| **State** | [`P0.state.json`](P0.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | ponytail; learn-while-building |

---

## Stop (required)

```text
rg -n "## User|## Job|## P0|## Non-goals" docs/planning/PRODUCT_KERNEL_HARDEN.md
```

Exit 0 = pass. Prose-only judgment is illegal.

---

## Objective

PRODUCT overlay locked for kernel-harden; base PRODUCT.md points at overlay without dropping UG EE user.

## Non-goals

- src/ edits
- H5 loop
- OTel

---

## Contract

**Input (lead → maker):**

```json
{ "node_id": "P0", "findings": [] }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "", "artifacts": [] }
```

**Checker output:**

```json
{ "pass": false, "command": "rg -n \"## User|## Job|## P0|## Non-goals\" docs/planning/PRODUCT_KERNEL_HARDEN.md", "exit_code": 1, "findings": [] }
```

---

## Maker procedure

1. Confirm GATE_0 on disk
2. Ensure PRODUCT_KERNEL_HARDEN has User/Job/Done/P0/P1/Non-goals/Honest holes
3. Add one-line pointer in docs/PRODUCT.md Later or P0 note for this graph
4. Do not edit runner

---

## Failure modes

- Missing headings
- Product code sneak

On fail with rounds left: remake using **checker findings only** (no whole-graph paste).

---

## Hardening notes

- No secrets
- Exact token unchecked

- Exact token `unchecked` never paraphrased.
- No H5 agent loop.
- No OpenTelemetry dependency.
- Maker must not be checker.

---

## Commits (this node)

Lead commits after **passed**, mapping to IMPLEMENTATION_PLAN / master §9 rows for this node. Subagents do not commit.

---

## Escalate

After `2` failures, or if Stop cannot run: set state `escalated`, stop dependents, ask human.

---

## Do not

- Commit / push / open PR from the maker Task
- Write outside **Write paths**
- Expand into another graph
- Load sibling loop plans (lead passes artifacts)
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files + notes. Checker: pass/exit/findings. Lead updates `P0.state.json` (`round`, `status`).
