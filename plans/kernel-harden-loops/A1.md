# Loop plan — `A1` — ADRs

> Extensive node plan for kernel-harden graph. Parent: [`LOOP_GRAPH.md`](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `A1` |
| **Job** | ADRs |
| **Wave** | 0 |
| **Depends on (data)** | P0 D0 |
| **Write paths** | `docs/planning/ADR_TRACE_JSONL.md DECISIONS.md` |
| **Read paths** | `docs/planning/GATE_0_KERNEL_HARDEN.md docs/planning/D0_GAPS_KERNEL_HARDEN.md` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **Inner critique loop** | no |
| **State** | [`A1.state.json`](A1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | ponytail; learn-while-building |

---

## Stop (required)

```text
rg -n "JSONL|no OTel|OpenTelemetry|custom adapter|held-out|frozen" docs/planning/ADR_TRACE_JSONL.md DECISIONS.md
```

Exit 0 = pass. Prose-only judgment is illegal.

---

## Objective

ADR_TRACE_JSONL accepted; DECISIONS.md records JSONL/no-OTel/custom Unagent export/held-out/frozen scorer.

## Non-goals

- Adding telemetry deps
- Changing gold scorer

---

## Contract

**Input (lead → maker):**

```json
{ "node_id": "A1", "findings": [] }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "", "artifacts": [] }
```

**Checker output:**

```json
{ "pass": false, "command": "rg -n \"JSONL|no OTel|OpenTelemetry|custom adapter|held-out|frozen\" docs/planning/ADR_TRACE_JSONL.md DECISIONS.md", "exit_code": 1, "findings": [] }
```

---

## Maker procedure

1. Finalize ADR schema v1
2. Append DECISIONS row
3. Confirm no pyproject OTel

---

## Failure modes

- Prose ADR without schema
- OTel allowed

On fail with rounds left: remake using **checker findings only** (no whole-graph paste).

---

## Hardening notes

- Frozen scorer called out
- Held-out ids policy

- Exact token `unchecked` never paraphrased.
- No H5 agent loop.
- No OpenTelemetry dependency.
- Maker must not be checker.

---

## Commits (this node)

Lead commits after **passed**, mapping to IMPLEMENTATION_PLAN / master §9 rows for this node. Subagents do not commit.

---

## Escalate

After `3` failures, or if Stop cannot run: set state `escalated`, stop dependents, ask human.

---

## Do not

- Commit / push / open PR from the maker Task
- Write outside **Write paths**
- Expand into another graph
- Load sibling loop plans (lead passes artifacts)
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files + notes. Checker: pass/exit/findings. Lead updates `A1.state.json` (`round`, `status`).
