# Loop plan — `U1` — trace UX IA

> Extensive node plan for kernel-harden graph. Parent: [`LOOP_GRAPH.md`](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `U1` |
| **Job** | trace UX IA |
| **Wave** | 0 |
| **Depends on (data)** | A1 |
| **Write paths** | `docs/planning/U1_TRACE_UX.md` |
| **Read paths** | `ui/src/slots/root.jsx src/electrical_engineer/ui_server/app.py docs/design/` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 2 |
| **Inner critique loop** | no |
| **State** | [`U1.state.json`](U1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | ponytail; learn-while-building |

---

## Stop (required)

```text
test -f docs/planning/U1_TRACE_UX.md && rg -n "run\.more|trace\.jsonl|unchecked" docs/planning/U1_TRACE_UX.md
```

Exit 0 = pass. Prose-only judgment is illegal.

---

## Objective

UX IA for human-readable observation excerpt + trace timeline in existing run.more / inspector; DESIGN-coinbase; no brand rewrite.

## Non-goals

- New visual brand
- Cards in hero

---

## Contract

**Input (lead → maker):**

```json
{ "node_id": "U1", "findings": [] }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "", "artifacts": [] }
```

**Checker output:**

```json
{ "pass": false, "command": "test -f docs/planning/U1_TRACE_UX.md && rg -n \"run\.more|trace\.jsonl|unchecked\" docs/planning/U1_TRACE_UX.md", "exit_code": 1, "findings": [] }
```

---

## Maker procedure

1. Map current observation <pre>
2. Specify excerpt fields: unchecked_reason, capabilities, last N spans
3. A11y: details/summary keyboard
4. Write U1_TRACE_UX.md

---

## Failure modes

- Requires new SPA router

On fail with rounds left: remake using **checker findings only** (no whole-graph paste).

---

## Hardening notes

- Bind stays 127.0.0.1
- No secrets in UI payload

- Exact token `unchecked` never paraphrased.
- No H5 agent loop.
- No OpenTelemetry dependency.
- Maker must not be checker.

---

## Commits (this node)

Lead commits after **passed**, mapping to IMPLEMENTATION_PLAN / master §9 rows for this node. Subagents do not commit.

---

## Escalate

After `2` failures, or if Stop cannot run: set state `escalated`, stop dependents, ask human.

---

## Do not

- Commit / push / open PR from the maker Task
- Write outside **Write paths**
- Expand into another graph
- Load sibling loop plans (lead passes artifacts)
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files + notes. Checker: pass/exit/findings. Lead updates `U1.state.json` (`round`, `status`).
