# Loop plan — `D0` — docs-in gaps

> Extensive node plan for kernel-harden graph. Parent: [`LOOP_GRAPH.md`](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `D0` |
| **Job** | docs-in gaps |
| **Wave** | 0 |
| **Depends on (data)** | P0 |
| **Write paths** | `docs/planning/D0_GAPS_KERNEL_HARDEN.md` |
| **Read paths** | `docs/ARCHITECTURE.md src/electrical_engineer/runner/execute.py` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 2 |
| **Inner critique loop** | no |
| **State** | [`D0.state.json`](D0.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | ponytail; learn-while-building |

---

## Stop (required)

```text
test -f docs/planning/D0_GAPS_KERNEL_HARDEN.md && rg -n "observation|trace|gold|UI" docs/planning/D0_GAPS_KERNEL_HARDEN.md
```

Exit 0 = pass. Prose-only judgment is illegal.

---

## Objective

Gap list names observation-only status, missing wall_ms, thin gold, UI raw dump, Unagent/Improveness not wired.

## Non-goals

- Implementing fixes
- Rewriting ARCHITECTURE wholesale

---

## Contract

**Input (lead → maker):**

```json
{ "node_id": "D0", "findings": [] }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "", "artifacts": [] }
```

**Checker output:**

```json
{ "pass": false, "command": "test -f docs/planning/D0_GAPS_KERNEL_HARDEN.md && rg -n \"observation|trace|gold|UI\" docs/planning/D0_GAPS_KERNEL_HARDEN.md", "exit_code": 1, "findings": [] }
```

---

## Maker procedure

1. Read execute.py observation writer
2. Read ui_server run_detail
3. List gaps for trace/gold/UI/export
4. Write D0_GAPS_KERNEL_HARDEN.md

---

## Failure modes

- Vague gaps without paths

On fail with rounds left: remake using **checker findings only** (no whole-graph paste).

---

## Hardening notes

- Cite file paths
- No invented capability ids

- Exact token `unchecked` never paraphrased.
- No H5 agent loop.
- No OpenTelemetry dependency.
- Maker must not be checker.

---

## Commits (this node)

Lead commits after **passed**, mapping to IMPLEMENTATION_PLAN / master §9 rows for this node. Subagents do not commit.

---

## Escalate

After `2` failures, or if Stop cannot run: set state `escalated`, stop dependents, ask human.

---

## Do not

- Commit / push / open PR from the maker Task
- Write outside **Write paths**
- Expand into another graph
- Load sibling loop plans (lead passes artifacts)
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files + notes. Checker: pass/exit/findings. Lead updates `D0.state.json` (`round`, `status`).
