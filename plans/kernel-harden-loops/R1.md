# Loop plan — `R1` — boot

> Extensive node plan for kernel-harden graph. Parent: [`LOOP_GRAPH.md`](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `R1` |
| **Job** | boot |
| **Wave** | 2 |
| **Depends on (data)** | E1 |
| **Write paths** | `docs/planning/R1_BOOT_KERNEL_HARDEN.md` |
| **Read paths** | `scripts/kernel_harden/trial_driver.py` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 2 |
| **Inner critique loop** | no |
| **State** | [`R1.state.json`](R1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | ponytail; learn-while-building |

---

## Stop (required)

```text
rg -n "exit|observation|trace" docs/planning/R1_BOOT_KERNEL_HARDEN.md
```

Exit 0 = pass. Prose-only judgment is illegal.

---

## Objective

Boot log records help, one run/trial_driver smoke, observation+trace paths, UI health if started.

## Non-goals

- Full corpus

---

## Contract

**Input (lead → maker):**

```json
{ "node_id": "R1", "findings": [] }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "", "artifacts": [] }
```

**Checker output:**

```json
{ "pass": false, "command": "rg -n \"exit|observation|trace\" docs/planning/R1_BOOT_KERNEL_HARDEN.md", "exit_code": 1, "findings": [] }
```

---

## Maker procedure

1. Run electrical-engineer --help
2. Smoke one scenario
3. Record exit codes and artifact paths
4. Optional UI /api/health

---

## Failure modes

- Missing trace path in log

On fail with rounds left: remake using **checker findings only** (no whole-graph paste).

---

## Hardening notes

- No secrets in boot log

- Exact token `unchecked` never paraphrased.
- No H5 agent loop.
- No OpenTelemetry dependency.
- Maker must not be checker.

---

## Commits (this node)

Lead commits after **passed**, mapping to IMPLEMENTATION_PLAN / master §9 rows for this node. Subagents do not commit.

---

## Escalate

After `2` failures, or if Stop cannot run: set state `escalated`, stop dependents, ask human.

---

## Do not

- Commit / push / open PR from the maker Task
- Write outside **Write paths**
- Expand into another graph
- Load sibling loop plans (lead passes artifacts)
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files + notes. Checker: pass/exit/findings. Lead updates `R1.state.json` (`round`, `status`).
