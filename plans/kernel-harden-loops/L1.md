# Loop plan — `L1` — learning sync

> Extensive node plan for kernel-harden graph. Parent: [`LOOP_GRAPH.md`](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `L1` |
| **Job** | learning sync |
| **Wave** | 7 |
| **Depends on (data)** | D1 |
| **Write paths** | `LEARNING.md` |
| **Read paths** | `docs/CASE_STUDY_DOMAIN_KERNEL.md docs/planning/**` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 2 |
| **Inner critique loop** | no |
| **State** | [`L1.state.json`](L1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | ponytail; learn-while-building |

---

## Stop (required)

```text
rg -n "Phase|Concept|Trade-off|Trade-off" LEARNING.md
```

Exit 0 = pass. Prose-only judgment is illegal.

---

## Objective

LEARNING.md covers W0–W7 with Concept/Pattern/Trade-off per wave cluster.

## Non-goals

- New features

---

## Contract

**Input (lead → maker):**

```json
{ "node_id": "L1", "findings": [] }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "", "artifacts": [] }
```

**Checker output:**

```json
{ "pass": false, "command": "rg -n \"Phase|Concept|Trade-off|Trade-off\" LEARNING.md", "exit_code": 1, "findings": [] }
```

---

## Maker procedure

1. Append phase entries for W0–W7
2. Include domain-kernel design lessons
3. Link case study

---

## Failure modes

- Missing Phase headings

On fail with rounds left: remake using **checker findings only** (no whole-graph paste).

---

## Hardening notes

- No secrets

- Exact token `unchecked` never paraphrased.
- No H5 agent loop.
- No OpenTelemetry dependency.
- Maker must not be checker.

---

## Commits (this node)

Lead commits after **passed**, mapping to IMPLEMENTATION_PLAN / master §9 rows for this node. Subagents do not commit.

---

## Escalate

After `2` failures, or if Stop cannot run: set state `escalated`, stop dependents, ask human.

---

## Do not

- Commit / push / open PR from the maker Task
- Write outside **Write paths**
- Expand into another graph
- Load sibling loop plans (lead passes artifacts)
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files + notes. Checker: pass/exit/findings. Lead updates `L1.state.json` (`round`, `status`).
