# Loop plan — `D1` — docs-out

> Extensive node plan for kernel-harden graph. Parent: [`LOOP_GRAPH.md`](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `D1` |
| **Job** | docs-out |
| **Wave** | 7 |
| **Depends on (data)** | H1 |
| **Write paths** | `docs/CASE_STUDY_DOMAIN_KERNEL.md docs/integrations/UNAGENT.md docs/integrations/IMPROVENESS.md docs/PRODUCT.md docs/ARCHITECTURE.md README.md PROGRESS.md docs/planning/ARCHITECTURE_CRITIQUE_KERNEL_HARDEN.md docs/planning/FAILURE_REPORT_KERNEL_HARDEN.md` |
| **Read paths** | `docs/planning/R1_BOOT_KERNEL_HARDEN.md artifacts/kernel-harden/**` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **Inner critique loop** | no |
| **State** | [`D1.state.json`](D1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | ponytail; learn-while-building |

---

## Stop (required)

```text
rg -n "Boot|Corpus|Unagent|Improveness" docs/CASE_STUDY_DOMAIN_KERNEL.md docs/integrations/UNAGENT.md docs/integrations/IMPROVENESS.md
```

Exit 0 = pass. Prose-only judgment is illegal.

---

## Objective

Publishable case study + integration docs + PRODUCT/ARCHITECTURE/README/PROGRESS sync describing as-built evidence.

## Non-goals

- Claiming unverified wins
- Writing README before evidence (evidence exists post H1)

---

## Contract

**Input (lead → maker):**

```json
{ "node_id": "D1", "findings": [] }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "", "artifacts": [] }
```

**Checker output:**

```json
{ "pass": false, "command": "rg -n \"Boot|Corpus|Unagent|Improveness\" docs/CASE_STUDY_DOMAIN_KERNEL.md docs/integrations/UNAGENT.md docs/integrations/IMPROVENESS.md", "exit_code": 1, "findings": [] }
```

---

## Maker procedure

1. Write CASE_STUDY_DOMAIN_KERNEL.md
2. Write integrations UNAGENT + IMPROVENESS
3. Finalize critique/failure report
4. Sync PRODUCT/ARCHITECTURE notes on trace
5. README pointer
6. PROGRESS completion

---

## Failure modes

- Missing Boot/Corpus tokens

On fail with rounds left: remake using **checker findings only** (no whole-graph paste).

---

## Hardening notes

- Docs match boot/trials
- Honest holes

- Exact token `unchecked` never paraphrased.
- No H5 agent loop.
- No OpenTelemetry dependency.
- Maker must not be checker.

---

## Commits (this node)

Lead commits after **passed**, mapping to IMPLEMENTATION_PLAN / master §9 rows for this node. Subagents do not commit.

---

## Escalate

After `3` failures, or if Stop cannot run: set state `escalated`, stop dependents, ask human.

---

## Do not

- Commit / push / open PR from the maker Task
- Write outside **Write paths**
- Expand into another graph
- Load sibling loop plans (lead passes artifacts)
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files + notes. Checker: pass/exit/findings. Lead updates `D1.state.json` (`round`, `status`).
