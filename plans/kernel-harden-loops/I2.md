# Loop plan — `I2` — patch round 2

> Extensive node plan for kernel-harden graph. Parent: [`LOOP_GRAPH.md`](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `I2` |
| **Job** | patch round 2 |
| **Wave** | 5 |
| **Depends on (data)** | X_UNAGENT X_IMPROV |
| **Write paths** | `src/electrical_engineer/** eval/gold/** docs/planning/IMPROVE_LOG.md` |
| **Read paths** | `artifacts/kernel-harden/unagent-report.md artifacts/kernel-harden/improveness-notes.md` |
| **Maker type** | generalPurpose |
| **Maker model** | inherit |
| **Checker type** | explore (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 4 |
| **Inner critique loop** | yes |
| **State** | [`I2.state.json`](I2.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | ponytail; learn-while-building |

---

## Stop (required)

```text
./scripts/validate.sh && rg -n "I2" docs/planning/IMPROVE_LOG.md
```

Exit 0 = pass. Prose-only judgment is illegal.

---

## Objective

Second improve cycle from advisor FlipToDet/ABSTAIN + Improveness notes; validate + I2 log.

## Non-goals

- H5
- Ignoring ABSTAIN and forcing flips

---

## Contract

**Input (lead → maker):**

```json
{ "node_id": "I2", "findings": [] }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "", "artifacts": [] }
```

**Checker output:**

```json
{ "pass": false, "command": "./scripts/validate.sh && rg -n \"I2\" docs/planning/IMPROVE_LOG.md", "exit_code": 1, "findings": [] }
```

---

## Maker procedure

1. Triage Unagent recommendations
2. Apply only evidenced flips
3. Gold locks
4. IMPROVE_LOG I2
5. Inner critique rounds

---

## Failure modes

- validate red

On fail with rounds left: remake using **checker findings only** (no whole-graph paste).

---

## Hardening notes

- Respect ABSTAIN
- Held-out intact

- Exact token `unchecked` never paraphrased.
- No H5 agent loop.
- No OpenTelemetry dependency.
- Maker must not be checker.

---

## Commits (this node)

Lead commits after **passed**, mapping to IMPLEMENTATION_PLAN / master §9 rows for this node. Subagents do not commit.

---

## Escalate

After `4` failures, or if Stop cannot run: set state `escalated`, stop dependents, ask human.

---

## Do not

- Commit / push / open PR from the maker Task
- Write outside **Write paths**
- Expand into another graph
- Load sibling loop plans (lead passes artifacts)
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files + notes. Checker: pass/exit/findings. Lead updates `I2.state.json` (`round`, `status`).
