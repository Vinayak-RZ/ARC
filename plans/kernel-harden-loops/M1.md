# Loop plan — `M1` — integrate

> Extensive node plan for kernel-harden graph. Parent: [`LOOP_GRAPH.md`](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `M1` |
| **Job** | integrate |
| **Wave** | 2 |
| **Depends on (data)** | B_TRACE B_CORPUS B_EXPORT B_UI |
| **Write paths** | `scripts/kernel_harden/trial_driver.py scripts/kernel_harden/__init__.py` |
| **Read paths** | `scripts/kernel_harden/** src/electrical_engineer/cli.py` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 2 |
| **Inner critique loop** | no |
| **State** | [`M1.state.json`](M1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | ponytail; learn-while-building |

---

## Stop (required)

```text
test -f scripts/kernel_harden/trial_driver.py && uv run electrical-engineer --help >/dev/null
```

Exit 0 = pass. Prose-only judgment is illegal.

---

## Objective

trial_driver.py can load a scenario, invoke CLI or in-process execute, write CORPUS row stub.

## Non-goals

- Full 100-run corpus
- UI redesign

---

## Contract

**Input (lead → maker):**

```json
{ "node_id": "M1", "findings": [] }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "", "artifacts": [] }
```

**Checker output:**

```json
{ "pass": false, "command": "test -f scripts/kernel_harden/trial_driver.py && uv run electrical-engineer --help >/dev/null", "exit_code": 1, "findings": [] }
```

---

## Maker procedure

1. Wire scenario loader
2. Call execute or CLI
3. Assert trace.jsonl exists after one smoke
4. Document usage in script docstring

---

## Failure modes

- Driver missing
- CLI broken

On fail with rounds left: remake using **checker findings only** (no whole-graph paste).

---

## Hardening notes

- Uses cwd runs/
- Fail closed on missing scenario

- Exact token `unchecked` never paraphrased.
- No H5 agent loop.
- No OpenTelemetry dependency.
- Maker must not be checker.

---

## Commits (this node)

Lead commits after **passed**, mapping to IMPLEMENTATION_PLAN / master §9 rows for this node. Subagents do not commit.

---

## Escalate

After `2` failures, or if Stop cannot run: set state `escalated`, stop dependents, ask human.

---

## Do not

- Commit / push / open PR from the maker Task
- Write outside **Write paths**
- Expand into another graph
- Load sibling loop plans (lead passes artifacts)
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files + notes. Checker: pass/exit/findings. Lead updates `M1.state.json` (`round`, `status`).
