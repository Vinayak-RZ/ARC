# Loop plan — `H1` — harden

> Extensive node plan for kernel-harden graph. Parent: [`LOOP_GRAPH.md`](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `H1` |
| **Job** | harden |
| **Wave** | 6 |
| **Depends on (data)** | T_FINAL |
| **Write paths** | `docs/planning/H1_HARDEN_KERNEL_HARDEN.md .gitignore` |
| **Read paths** | `scripts/validate.sh artifacts/kernel-harden/**` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore (readonly) |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **Inner critique loop** | no |
| **State** | [`H1.state.json`](H1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | ponytail; learn-while-building |

---

## Stop (required)

```text
./scripts/validate.sh && ! rg -n "0\.0\.0\.0" src ui --glob "!**/test*" && test -f docs/planning/H1_HARDEN_KERNEL_HARDEN.md
```

Exit 0 = pass. Prose-only judgment is illegal.

---

## Objective

Validate green; no 0.0.0.0; secret scan clean; held-out integrity; raw gitignored; H1 log.

## Non-goals

- Feature work
- Docs case study (D1)

---

## Contract

**Input (lead → maker):**

```json
{ "node_id": "H1", "findings": [] }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "", "artifacts": [] }
```

**Checker output:**

```json
{ "pass": false, "command": "./scripts/validate.sh && ! rg -n \"0\.0\.0\.0\" src ui --glob \"!**/test*\" && test -f docs/planning/H1_HARDEN_KERNEL_HARDEN.md", "exit_code": 1, "findings": [] }
```

---

## Maker procedure

1. validate.sh
2. Confirm bind grep
3. Scan committed artifacts for keys
4. Ensure gitignore for artifacts/kernel-harden/raw/
5. Verify HELD_OUT_IDS absent from IMPROVE_LOG prompts section
6. Write H1_HARDEN log

---

## Failure modes

- validate red
- 0.0.0.0 present

On fail with rounds left: remake using **checker findings only** (no whole-graph paste).

---

## Hardening notes

- Full H1 checklist from master plan

- Exact token `unchecked` never paraphrased.
- No H5 agent loop.
- No OpenTelemetry dependency.
- Maker must not be checker.

---

## Commits (this node)

Lead commits after **passed**, mapping to IMPLEMENTATION_PLAN / master §9 rows for this node. Subagents do not commit.

---

## Escalate

After `3` failures, or if Stop cannot run: set state `escalated`, stop dependents, ask human.

---

## Do not

- Commit / push / open PR from the maker Task
- Write outside **Write paths**
- Expand into another graph
- Load sibling loop plans (lead passes artifacts)
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files + notes. Checker: pass/exit/findings. Lead updates `H1.state.json` (`round`, `status`).
