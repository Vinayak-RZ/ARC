# Loop plan — `T1` — kernel and host trials

> Collapsed plan for **one loop node**. Parent: [LOOP_GRAPH.md](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `T1` |
| **Job** | Kernel MATLAB/adapter trials plus host-harness one question per core pack. |
| **Wave** | 5 |
| **Depends on (data)** | R1 |
| **Write paths** | `docs/planning/T1_TRIALS.md`, `docs/planning/T1_HOST.md`, skill/adapter fixes if a trial fails |
| **Read paths** | `skills/`, MCP, `knowledge/ug-ee/` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **State** | [T1.state.json](T1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | none |

---

## Stop (required)

**Command:**

```text
test -f docs/planning/T1_HOST.md && grep -c '| pass' docs/planning/T1_HOST.md | awk '{exit($1<10)}'
```

---

## Objective

T1_HOST ≥10 pack rows; T1_TRIALS kernel queue filled.

## Non-goals

- Scoring LLM eloquence
- Committing passwords

---

## Contract

**Input:** `docs/planning/GATE_0_SKILLS_MATLAB.md` plus upstream artifacts.

**Maker output:** files_touched + notes. **Do NOT commit.**

**Checker output:** `{ pass, command, exit_code, findings }`. Readonly.

---

## Escalate

After 3 failed rounds, status `escalated`; wait for the human.

---

## Commits (this node only)

21: test(host) T1_HOST — lead commits after **passed**.

---

## Do not

- Commit, push, or open a PR
- Write outside write paths
- Checker: write product files or be the maker
- Expand into another graph
- Use Cursor `/loop` timers
