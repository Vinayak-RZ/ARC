# Loop plan — `H1` — whole-tree harden

> Collapsed plan for **one loop node**. Parent: [LOOP_GRAPH.md](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `H1` |
| **Job** | Ponytail-review, secrets, bind, leftover defects, retest. |
| **Wave** | 8 |
| **Depends on (data)** | D1 |
| **Write paths** | leftover defects in-diff; `docs/planning/PONYTAIL_REVIEW.md` (this graph section) |
| **Read paths** | branch diff |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **State** | [H1.state.json](H1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | ponytail-review |

---

## Stop (required)

**Command:**

```text
./scripts/validate.sh && ! git grep -n 'Raizada@' -- ':!docs/planning/GATE_0_SKILLS_MATLAB.md' ':!plans/skills-matlab-loops/H1.md' | grep .; python -m pytest tests/integration/test_ui_a11y.py -q
```

---

## Objective

validate green; no secrets; honest holes labeled.

## Non-goals

- Drive-by rewrite of unrelated history

---

## Contract

**Input:** `docs/planning/GATE_0_SKILLS_MATLAB.md` plus upstream artifacts.

**Maker output:** files_touched + notes. **Do NOT commit.**

**Checker output:** `{ pass, command, exit_code, findings }`. Readonly.

---

## Escalate

After 3 failed rounds, status `escalated`; wait for the human.

---

## Commits (this node only)

24: chore H1 harden — lead commits after **passed**.

---

## Do not

- Commit, push, or open a PR
- Write outside write paths
- Checker: write product files or be the maker
- Expand into another graph
- Use Cursor `/loop` timers
