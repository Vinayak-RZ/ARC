# Loop plan — `A1` — ADRs

> Collapsed plan for **one loop node**. Parent: [LOOP_GRAPH.md](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `A1` |
| **Job** | ADR-0015 pack specialists; ADR-0016 Arc-mediated MATLAB MCP; cite R0_SOLUTIONS. |
| **Wave** | 1 |
| **Depends on (data)** | R_SURVEY, P0 |
| **Write paths** | `DECISIONS.md` |
| **Read paths** | `docs/planning/R0_SOLUTIONS.md`, `docs/PRODUCT.md` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **State** | [A1.state.json](A1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | system-design-tradeoffs |

---

## Stop (required)

**Command:**

```text
grep -q 'ADR-0015' DECISIONS.md && grep -q 'ADR-0016' DECISIONS.md && grep -q 'R0_SOLUTIONS' DECISIONS.md
```

---

## Objective

Both ADRs accepted with trust (single-seat MATLAB, no EE agents in product .cursor/).

## Non-goals

- Implementing the client or adapters

---

## Contract

**Input:** `docs/planning/GATE_0_SKILLS_MATLAB.md` plus upstream artifacts.

**Maker output:** files_touched + notes. **Do NOT commit.**

**Checker output:** `{ pass, command, exit_code, findings }`. Readonly.

---

## Escalate

After 3 failed rounds, status `escalated`; wait for the human.

---

## Commits (this node only)

6: docs(decisions) ADR-0015 + ADR-0016 — lead commits after **passed**.

---

## Do not

- Commit, push, or open a PR
- Write outside write paths
- Checker: write product files or be the maker
- Expand into another graph
- Use Cursor `/loop` timers
