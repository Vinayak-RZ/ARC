# Loop plan — `D1` — docs-out

> Collapsed plan for **one loop node**. Parent: [LOOP_GRAPH.md](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `D1` |
| **Job** | Document what actually ran: install, mediation, boot command. |
| **Wave** | 7 |
| **Depends on (data)** | U_POLISH |
| **Write paths** | `README.md`, `docs/hosts/`, `docs/ON_THE_HARNESS.md`, `docs/EXTENSIVE.md` if needed |
| **Read paths** | T1/R1/U_CRITIQUE evidence |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **State** | [D1.state.json](D1.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | readme |

---

## Stop (required)

**Command:**

```text
grep -q 'hosts install' README.md && grep -q 'MATLAB' docs/ON_THE_HARNESS.md
```

---

## Objective

README names install + mediation + boot.

## Non-goals

- Docs before evidence (already past T1)

---

## Contract

**Input:** `docs/planning/GATE_0_SKILLS_MATLAB.md` plus upstream artifacts.

**Maker output:** files_touched + notes. **Do NOT commit.**

**Checker output:** `{ pass, command, exit_code, findings }`. Readonly.

---

## Escalate

After 3 failed rounds, status `escalated`; wait for the human.

---

## Commits (this node only)

23: docs-out — lead commits after **passed**.

---

## Do not

- Commit, push, or open a PR
- Write outside write paths
- Checker: write product files or be the maker
- Expand into another graph
- Use Cursor `/loop` timers
