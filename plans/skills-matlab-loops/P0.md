# Loop plan — `P0` — product lock

> Collapsed plan for **one loop node**. Parent: [LOOP_GRAPH.md](../../LOOP_GRAPH.md).

| Field | Value |
|-------|-------|
| **Node id** | `P0` |
| **Job** | Move specialists + Arc-mediated MATLAB + host-harness/UI polish into this-graph P0. |
| **Wave** | 1 |
| **Depends on (data)** | R_SURVEY / docs/planning/R0_SOLUTIONS.md |
| **Write paths** | `docs/PRODUCT.md` |
| **Read paths** | `docs/planning/GATE_0_SKILLS_MATLAB.md`, `docs/planning/R0_SOLUTIONS.md` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | explore |
| **Checker model** | composer-2.5-fast else inherit |
| **Max rounds** | 3 |
| **State** | [P0.state.json](P0.state.json) |
| **Isolation** | path-ownership; checker writes nothing in product trees |
| **Companion skills** | none |

---

## Stop (required)

**Command:**

```text
grep -q '## User' docs/PRODUCT.md && grep -q '## Job' docs/PRODUCT.md && grep -q '## P0' docs/PRODUCT.md && grep -q '## Non-goals' docs/PRODUCT.md && grep -q 'mediat' docs/PRODUCT.md
```

---

## Objective

docs/PRODUCT.md headings + mediation language.

## Non-goals

- Replacing DESIGN-coinbase
- Electives as first-class packs

---

## Contract

**Input:** `docs/planning/GATE_0_SKILLS_MATLAB.md` plus upstream artifacts.

**Maker output:** files_touched + notes. **Do NOT commit.**

**Checker output:** `{ pass, command, exit_code, findings }`. Readonly.

---

## Escalate

After 3 failed rounds, status `escalated`; wait for the human.

---

## Commits (this node only)

5: docs(product) overlay — lead commits after **passed**.

---

## Do not

- Commit, push, or open a PR
- Write outside write paths
- Checker: write product files or be the maker
- Expand into another graph
- Use Cursor `/loop` timers
