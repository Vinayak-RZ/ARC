# D0 gaps — Simulink + host-agent catalog

Gaps against P0 after skills-matlab archive.

- README first screen counts packs/recipes/checks, not spawnable specialists
- `docs/ON_THE_HARNESS.md` still says Simulink Agentic Toolkit is later
- Host docs forbid peer MATLAB MCP but do not name Simulink `tools.json` / `model_*`
- `hosts/adapters/specialist-body.md` forbids MATLAB host tools, not Simulink `model_*` or Cloud worktree spawn
- No `hosts/agents/INDEX.md` for clone search
- `run-simulink-if-present` does not exist; `matlab_mcp.spawn_cmd` omits `--extension-file`
- UI has MATLAB chip only
