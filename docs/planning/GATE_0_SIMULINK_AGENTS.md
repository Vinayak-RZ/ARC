# Gate 0 — Simulink toolkit + host-spawned EE specialists

**Status:** Closed 2026-09-14. Do not re-ask on resume.

Skill: `graph-engineering` + `nawab-plans` lite + `agentic-system-design`.
XOR: not graphify. Do not reopen D19, knowledge-corpus, or the archived skills-matlab graph except as a pointer.

Archives: [`LOOP_GRAPH_SKILLS_MATLAB.md`](LOOP_GRAPH_SKILLS_MATLAB.md), [`LOOP_GRAPH_D19.md`](LOOP_GRAPH_D19.md), [`LOOP_GRAPH_KNOWLEDGE.md`](LOOP_GRAPH_KNOWLEDGE.md).

## Research snapshot

Checkout includes host-native pack specialists and Arc-mediated MATLAB MCP (`run-matlab-if-present`). Product `.cursor/` is coding SDLC. This Cloud VM has no `matlab` and no `matlab-mcp-server`. Simulink Agentic Toolkit was Later on the previous graph.

OpenMontage (calesthio/OpenMontage): the rented coding assistant is the control plane; Python is tools + persistence. Domain cards live in-repo; host dirs are install targets. SDLC orchestrators (48–88 generic roles, “77 skills”) are not the analog.

## Locked from PID/PRD (cited, not re-asked)

- H3; H5 Python specialist fan-out forbidden
- EE packs never in this product repo’s `.cursor/`
- Product/CI work with zero MATLAB
- Do not invent capability ids; `CD-SIMULINK-PLANT` stays
- DESIGN-coinbase; never store MathWorks passwords

## Owner answers (2026-09-14)

1. **Loop owner:** rented host (Cursor / Codex / Claude) spawns children. Not an Arc-owned supervisor.
2. **Where agents live:** canonical catalog in `hosts/agents/` (OpenMontage-style). Install copies into homework host dirs. Not product `.cursor/agents/`.
3. **Commit budget:** 12 (default).
4. **Inventory:** 11 pack specialists + `ee-simulink` = 12.
5. **Live MATLAB/Simulink:** later; this graph is kernel mediation + fail-closed stub.

## P0 vs later

**P0:** in-repo agent catalog; host-optimized spawn cards; kernel `--extension-file` + `run-simulink-if-present`; UI chip; README layered counts.

**Later:** live MATLAB/Simulink on a licensed desktop; real retrieval; electives.
