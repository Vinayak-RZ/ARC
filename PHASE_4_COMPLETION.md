# PHASE 4 COMPLETE — B_ACI + B_WF + B_UI + B_PACKS

## Completed work

5–7 MCP verbs including `propose_composition`; CLI omitted-id classifier; host-path YAML without `solve-explain`; two-band UI slots; pack coverage lock.

## Validation

Wave 4 stop pytest files green. Full suite: one known fail in rag-retrieval gold (FR9 — B_EVAL).

## What you learned

- Host ACI is verb kinds, not 1:1 provider tools; `run_workflow` is rollback.
- Capability ids in YAML are replay bindings; the kernel rebinds before run.
- The argument band can sit next to evidentiary JSON without owning the badge.
- Pack coverage is “named capability or cannot-do”, not a gold item per genre.
